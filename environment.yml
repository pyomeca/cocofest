# create a dev conda environment with: `make create_env` or `conda env create -f environment.yml`
name: cocofest
channels:
- conda-forge
dependencies:
- bioptim=3.4.0
- bioviz
- python-graphviz
- pyqtgraph
- pyorerun
- rerun-sdk=0.21.0
- scikit-optimize
