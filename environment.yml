# create a dev conda environment with: `conda env create -f environment.yml` (see README.md "Installation")
name: cocofest
channels:
- conda-forge
dependencies:
- bioptim=3.4.0
- bioviz
- python-graphviz
- pyqtgraph
- pyorerun
- rerun-sdk=0.21.0
- scikit-optimize
